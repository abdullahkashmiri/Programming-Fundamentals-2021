/*#include<iostream>
using namespace std;

float calculateretail(float cost, float markup)
{
	float result;
	result = cost + (cost * markup) / 100;
	return result;
}
int main()
{
	float res, cost, markup;
	cout << "Enter the Wholsale cost of the Product : ";
	cin >> cost;
	cout << "Enter the Markup Percentage on the Product : ";
	cin >> markup;
	if (cost < 0 || markup < 0)
		cout << "Invalid Inputs ";
	else
		res = calculateretail(cost, markup);
	cout << "The Retail price is : " << res;
	system("pause");
	return 0;
}*/