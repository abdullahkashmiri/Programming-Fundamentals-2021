/*Write a function that accepts a C-string as an argument and displays its contents backward. For
instance, if the string argument is “Gravity” the function should display “ ytivarG ”. Demonstrate
the function in a program that asks the user to input a string and then passes it to the function.*/

/*
#include<iostream>
using namespace std;

void countsize(char array[], int size, int count)
{
    while (array[count] != '\0')
    {
        count++;
    }

    for (int i = count; count >= 0; count--)
    {
        cout << array[count];
    }
}

int main()
{
    const int size = 20;
    char array[size];
    int count = 0;
    cout << "Enter a Sentence not More than " << size - 1 << " characters : ";
    cin.getline(array, size);
    countsize(array, size, count);
    system("pause");
    return 0;
}
*/