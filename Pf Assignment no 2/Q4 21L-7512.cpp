//Find three consecutive numbers whose multiplication is equal to the Required Number, (Your program should prompt for the number Nand outputs 3 - numbers those if we multiply equals to N.Also if there isn’t any 3–tuple of numbers then it should say NO).
/*#include<iostream>
using namespace std;
int main()
{
	int num;
	cout << "Enter the Number : ";
	cin >> num;

	for (int n = 1; n < num; n++)
	{
		// Condition for Checking Product of three consecutive numbers 
		if (((n) * (n + 1) * (n + 2)) == (num))
		{
			cout << "Three Consecutive numbers are : " << n << "," << n + 1 << "," << n + 2 << endl;
			return 0;
		}
	}
	cout << "No Consective Multiples ";
	return 0;
}
*/