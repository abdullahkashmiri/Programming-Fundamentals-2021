//Write a C++ program that draws a sideways tree pointing right; for example, if the user enters 7, the program would print.
/*#include<iostream>
using namespace std;
int main()
{
	int size,i;
	cout << "Enter the Size of the Sideway Tree : ";
	cin >> size;
	for (int x=1; x <= size; x++)
	{
		for (int y = 1; y <= x; y++)
		{
			cout << "*";
	    }
		cout << endl;
	}
	for (int x = size - 1; x <= size && x!=0; --x)
	{
		for(int y = x ;y<=size && y!=0; --y)
		{
			cout << "*";
		}
		cout << endl;
	}
	return 0;
}*/